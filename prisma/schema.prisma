generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model PinnedSong {
  id        String   @id
  title     String
  artist    String
  thumbnail String
  duration  String
  createdAt DateTime @default(now())
}